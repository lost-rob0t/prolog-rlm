# Copy to .env and fill in. .env is git-ignored.

# Credential resolved by rlm_chain:openrouter_provider/2 as env('OPENROUTER_API_KEY').
OPENROUTER_API_KEY=

# Optional: overrides rlm_chain:default_openrouter_model/1 (defaults to openrouter/free).
OPENROUTER_TEST_MODEL=openrouter/free
