name: Issue12 dict hardening

on:
  push:
    branches: [feature/issue-12-durable-graph]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'Apply issue12 dict hardening'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: python3 scripts/issue12_dict_patch.py
      - name: Commit patch
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          git config user.name "nsaspy"
          git config user.email "104283403+lost-rob0t@users.noreply.github.com"
          git add prolog/rlm_graph.pl
          git commit -m "Harden graph dict access"
          git push origin HEAD:${GITHUB_REF_NAME}
