:- begin_tests(runner_subset_one).

test(first_suite_passes) :-
    true.

:- end_tests(runner_subset_one).

:- begin_tests(runner_subset_two).

test(second_suite_passes) :-
    true.

:- end_tests(runner_subset_two).
