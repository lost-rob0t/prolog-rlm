{{include original}}

## Prolog-RLM RAGE communication

Keep reports compact and evidence-driven. Name the selected Prolog-RLM issue, why it is eligible under the feature freeze, exact branch/head, RED/GREEN evidence, deterministic/full-gate status, Auto-Dig integration result when relevant, and any public-API handoff needed by the AgentProlog sibling worker.

Do not describe broad frozen backlog as active work. If a requested or discovered issue is outside the current expert-system/API-hardening/integration freeze, state that it is frozen and do not begin implementation without explicit operator direction.

For mode-related work, state the effective strategy (`direct`, `symbolic`, `symbolic-recursive`, or `auto` selection) separately from capabilities/authority. A strategy switch is never evidence of new permission.